<UserControl x:Class="MTGSetGenerator.SelectSetRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="0 0 0 5" Orientation="Horizontal" VerticalAlignment="Center">
        <RadioButton Name="rb_Select" GroupName="Sets" VerticalAlignment="Center" >
            <StackPanel Orientation="Horizontal" >
                <Grid Name="g_SetIcon" Margin="5 0 5 0">
                    <Image Grid.Column="2" Width="22px" Source="{StaticResource img_SetIconBackground}" />
                    <Image Name="img_SetIcon" Width="20px" Source="{StaticResource img_DarksignIcon}" />
                </Grid>
                <TextBlock Name="tbl_SetName" Style="{StaticResource tbl_Default}">Dark Souls: Prepare to Magic Edition</TextBlock>
            </StackPanel>
        </RadioButton>
    </StackPanel>
</UserControl>
